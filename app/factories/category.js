angular.module('app').factory('Category', function(){
  /*
   * Category Class
   */
  function Category(name, color){
    this.text = text;
    this.answer = answer;
    this.categoryId = categoryId;
  }

  return Category;
})
